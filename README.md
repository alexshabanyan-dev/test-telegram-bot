# Telegram Channel Monitor - MVP

Простая программа для мониторинга открытых Telegram каналов и поиска ключевых фраз в сообщениях.

## 🚀 Быстрый старт

### 1. Установка зависимостей

Создайте виртуальное окружение и установите зависимости:

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

> **Примечание**: На macOS с Homebrew Python обязательно используйте виртуальное окружение.

### 2. Получение API ключей

1. Перейдите на https://my.telegram.org/apps
2. Войдите используя свой номер телефона
3. Создайте новое приложение
4. Скопируйте `api_id` и `api_hash`

### 3. Настройка

1. Скопируйте `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Откройте `.env` и заполните:
   - `API_ID` - ваш API ID из шага 2
   - `API_HASH` - ваш API Hash из шага 2
   - `CHANNEL_NAME` - имя или username канала (например: `@channel_name` или `channel_name`)

3. (Опционально) Отредактируйте `config.py` для изменения списка ключевых слов

### 4. Запуск

Убедитесь, что виртуальное окружение активировано, затем запустите:

```bash
source venv/bin/activate  # если еще не активировано
python main.py
```

При первом запуске вам будет предложено ввести номер телефона и код подтверждения из Telegram.

## 📝 Настройка ключевых слов

Отредактируйте файл `config.py` и измените список `KEYWORDS`:

```python
KEYWORDS = [
    "вакансия",
    "работа",
    "важно",
    # добавьте свои ключевые слова
]
```

## 🔔 Уведомления

В текущей MVP версии уведомления выводятся в консоль. При нахождении совпадения вы увидите:
- Время обнаружения
- Название канала
- Найденные ключевые слова
- Текст сообщения

## 📋 Требования

- Python 3.7+
- Telegram аккаунт
- API credentials от Telegram

## 🔒 Безопасность

⚠️ **Важно**: Не коммитьте файл `.env` в git! Он уже добавлен в `.gitignore`.

## 🛠️ Дальнейшее развитие

Возможные улучшения:
- Отправка уведомлений в другой Telegram канал/чат
- Сохранение истории найденных сообщений
- Более сложные правила поиска (регулярные выражения)
- Веб-интерфейс для управления
- Фильтрация по дате/времени

